import React from "react";
import Trips from "./componentes/Trips";

export const metadata = {
    title: "About"
}

const Page = () => {
    return (
    <div>
        <Trips />
    </div>
    )
};

export default Page;